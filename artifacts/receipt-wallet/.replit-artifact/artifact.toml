kind = "web"
previewPath = "/"
title = "Receipt-to-Wallet"
version = "1.0.0"
id = "artifacts/receipt-wallet"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20254

[services.development]
run = "pnpm --filter @workspace/receipt-wallet run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/receipt-wallet", "run", "build" ]
publicDir = "artifacts/receipt-wallet/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20254"
BASE_PATH = "/"
